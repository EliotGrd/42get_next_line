
Loop sur GNL return une ligne a la fois

Retourne la ligne ou null si plus rien a lire ou error

Doit marcher en lisant un fichier et sur stdin

Ligne doit finir par \n a par si la fin du texte n'en a pas

Compile avec -D BUFFER_SIZE=n qui define la taille du buffer du read
Avoir une size par defaut pour que le programme soit compilable sans le param

Lire petit a petit

Bien savoir comment le programme se comporte avec differents buffer size

Behaviors :

Undefined quand la file en input est modifiée entre les calls (apres un call et tant que read est pas arrive a la fin du fichier)

Undefined quand tu essaie de lire un binaire

INTERDIT :

lseek()
Global variables

AUTORISE : 

read
malloc/free


PREPARER UN BON MAIN



char *get_next_line(int fd);

parcourir jusqu'a \n ou valeur zero de return de read

proteger le fd null
